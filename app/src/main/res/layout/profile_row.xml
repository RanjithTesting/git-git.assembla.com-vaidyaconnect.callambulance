<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/profileRowParentLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:orientation="horizontal"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"

    >

    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:gravity="center_vertical"

        >
        <TableRow
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >
        <TextView
            android:id="@+id/key"
            style="@style/tv_style1"
            android:layout_width="0dp"
            android:text="Title"
            android:layout_weight="1"
            />

        <TextView
            android:id="@+id/value"
            style="@style/tv_style1_light"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="Value"
            android:gravity="right"
            android:textStyle="normal"
             />
        </TableRow>
    </TableLayout>
    <ImageView
        android:id="@+id/patientCall"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:src="@drawable/emergency_calling"
        android:tint="@color/colorPrimary"
        android:contentDescription="@string/callNumber"
        android:visibility="gone"
        android:focusable="false"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ImageView
        android:id="@+id/patientMsg"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:background="@android:drawable/ic_dialog_email"
        android:backgroundTint="@color/colorPrimary"
        android:contentDescription="Message"
        android:focusable="false"
        android:visibility="gone"
        android:layout_alignTop="@+id/patientCall"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

</LinearLayout>